const { createClient } = require('@supabase/supabase-js')
require('dotenv').config({ path: '.env.local' })

async function updateSchema() {
    const url = process.env.NEXT_PUBLIC_SUPABASE_URL
    const key = process.env.SUPABASE_SERVICE_ROLE_KEY
    const supabase = createClient(url, key)

    const sql = `
        ALTER TABLE social_metrics ADD COLUMN IF NOT EXISTS reach INTEGER DEFAULT 0;
        ALTER TABLE social_metrics ADD COLUMN IF NOT EXISTS ctr DECIMAL DEFAULT 0;
        ALTER TABLE social_metrics ADD COLUMN IF NOT EXISTS views_3s INTEGER DEFAULT 0;
        ALTER TABLE social_metrics ADD COLUMN IF NOT EXISTS views_1min INTEGER DEFAULT 0;
        ALTER TABLE social_metrics ADD COLUMN IF NOT EXISTS for_you_rate DECIMAL DEFAULT 0;
        ALTER TABLE social_metrics ADD COLUMN IF NOT EXISTS shorts_views INTEGER DEFAULT 0;
        ALTER TABLE social_metrics ADD COLUMN IF NOT EXISTS video_engagement_rate DECIMAL DEFAULT 0;
        ALTER TABLE social_metrics ADD COLUMN IF NOT EXISTS shorts_engagement_rate DECIMAL DEFAULT 0;
        ALTER TABLE social_metrics ADD COLUMN IF NOT EXISTS avg_video_views INTEGER DEFAULT 0;
        ALTER TABLE social_metrics ADD COLUMN IF NOT EXISTS avg_shorts_views INTEGER DEFAULT 0;
        ALTER TABLE social_metrics ADD COLUMN IF NOT EXISTS story_reach INTEGER DEFAULT 0;
        ALTER TABLE social_metrics ADD COLUMN IF NOT EXISTS estimated_price_post DECIMAL(15,2) DEFAULT 0;
        ALTER TABLE social_metrics ADD COLUMN IF NOT EXISTS estimated_price_story DECIMAL(15,2) DEFAULT 0;
        ALTER TABLE social_metrics ADD COLUMN IF NOT EXISTS platform_revenue DECIMAL(15,2) DEFAULT 0;
    `
    
    // I don't have a way to run raw SQL directly unless I use the UI or an RPC.
    console.log('Please run this SQL in your Supabase Editor to add missing columns:')
    console.log(sql)
}

updateSchema()
